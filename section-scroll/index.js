import { Scroll } from './two.5.js';

const gui = new dat.gui.GUI();

const config = {
    'section 1': {
        photo: {

        }
    }
};

gui.remember(config);

const scroll = new Scroll({

});
